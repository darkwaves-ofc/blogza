model Feedback {
    id        String   @id @default(auto()) @map("_id") @db.ObjectId
    stars     Int
    email     String
    name      String
    feedback  String
    createdAt DateTime @default(now())
}
